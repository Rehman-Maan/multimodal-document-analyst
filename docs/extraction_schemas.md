# Extraction Schemas

Schemas are workspace-scoped JSON definitions with a `fields` list. Each field includes:

- `name`
- `type`
- `required`

Supported types are `string`, `decimal`, `date`, `integer`, and `boolean`.

Default receipt and invoice schemas are created for new workspaces and can also be seeded from the schema library.
